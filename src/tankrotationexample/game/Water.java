package tankrotationexample.game;

import java.awt.*;
import java.awt.image.BufferedImage;

public class Water extends Obstacle {
    public Water(int x, int y, BufferedImage img) {
        super(x, y, img);
    }
}
